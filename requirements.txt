openai
langchain
python-dotenv
streamlit
pyPDF2
langchain_community


-e . 
# Local package dependencies
# The above line allows you to install the package in editable mode, which is useful for local development.
